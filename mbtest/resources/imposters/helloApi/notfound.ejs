function (request, state, logger) {
    let response = JSON.parse("<%- stringify(filename, 'helloApi/2.json') %>");
        
    return {
        statusCode : 404,
        headers: {
            'Content-Type': 'application/json; charset=utf-8'
        },
        body: response
    };
}