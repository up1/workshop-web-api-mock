function (request, state, logger) {
    let response = JSON.parse("<%- stringify(filename, 'helloApi/1.json') %>");
        
    return {
        statusCode : 200,
        headers: {
            'Content-Type': 'application/json; charset=utf-8'
        },
        body: response
    };
}